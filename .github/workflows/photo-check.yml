name: Photo Link Checker

on:
  # Run weekly on Monday at 06:00 UTC
  schedule:
    - cron: '0 6 * * 1'
  # Run on PRs that touch speciesData.js
  pull_request:
    paths:
      - 'js/speciesData.js'
  # Allow manual trigger
  workflow_dispatch:

jobs:
  check-photos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Extract and check PHOTO_MAP URLs
        run: |
          # Extract URLs from PHOTO_MAP in speciesData.js
          grep -o "url:'[^']*'" js/speciesData.js | sed "s/^url:'//;s/'$//" > /tmp/photo_urls.txt

          total=$(wc -l < /tmp/photo_urls.txt)
          echo "Found $total photo URLs to check"
          echo ""

          broken=0
          checked=0

          while IFS= read -r url; do
            checked=$((checked + 1))
            # Use curl with redirect follow, 10s timeout, HEAD request
            status=$(curl -o /dev/null -s -w '%{http_code}' --head -L --max-time 10 "$url" 2>/dev/null || echo "000")

            if [ "$status" -ge 200 ] && [ "$status" -lt 400 ]; then
              echo "✅ [$status] $url"
            else
              echo "❌ [$status] $url"
              broken=$((broken + 1))
            fi

            # Be polite to Wikimedia servers
            sleep 0.3
          done < /tmp/photo_urls.txt

          echo ""
          echo "=============================="
          echo "Checked: $checked / $total"
          echo "Broken:  $broken"
          echo "=============================="

          if [ "$broken" -gt 0 ]; then
            echo ""
            echo "⚠️  $broken broken photo link(s) found. Update PHOTO_MAP in js/speciesData.js."
            exit 1
          fi

          echo "✅ All photo links are valid."
