# Prompt Archive

This directory stores archived prompt files from each development session.

Each file is named by prompt number and topic, e.g.:
- `PROMPT_00_baseline_audit.md`
- `PROMPT_01_extract_tree_data.md`

The prompts document what was requested and what was changed in each session.
