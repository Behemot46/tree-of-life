{
  "name": "tree-of-life-scripts",
  "version": "1.0.0",
  "private": true,
  "description": "Dev scripts for Tree of Life — image generation pipeline",
  "type": "module",
  "scripts": {
    "generate": "node generate-images.js",
    "generate:gemini": "node generate-images.js --provider gemini",
    "generate:openai": "node generate-images.js --provider openai",
    "generate:stability": "node generate-images.js --provider stability",
    "generate:replicate": "node generate-images.js --provider replicate"
  },
  "dependencies": {
    "sharp": "^0.33.0"
  }
}
